#! /usr/bin/env node

const shell = require("shelljs");
const argv = process.argv;

const args = argv.slice(2).join(' ');
const path = `${(argv[1].split('/').slice(0, argv[1].split('/').length - 1)).join('/')}/../rebar3`;

shell.exec(`${path}/rebar3 ${args}`);
