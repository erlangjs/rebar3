#! /usr/bin/env node

const shell = require("shelljs");
const argv = process.argv;

const args = argv.slice(0, 2).join(' ');
const path = `${argv[1].split('/').pop().join('/')}/../rebar3`;

shell.exec(`${path}/rebar3 ${args}`);
